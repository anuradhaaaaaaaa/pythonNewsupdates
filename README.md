Gold News Daily — Ranked by Impact

A simple Flask web app that shows the top 5 daily news articles about gold, ranked by their potential impact on gold prices.

The app uses the GNews API to fetch news and automatically caches it daily, so page refreshes won’t hit API limits.
